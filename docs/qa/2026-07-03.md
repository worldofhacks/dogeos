# QA 2026-07-03

> **DEPLOY BLOCKED — READ FIRST.** Today's deploy was blocked by the session
> permission system. https://dogeswap.ag is still running **yesterday's build**
> (pre-merge of today's nine PRs; main is at `12c063a`). Every live-site
> failure below that today's merges already fix is marked
> **"fixed on main, pending deploy"** and was NOT filed as a new issue.
> Re-run the live sweep after the next deploy.

Targets:
- Local (main @ `12c063a`): `http://127.0.0.1:8788` (e2e) / `http://127.0.0.1:8790` (perf harness — same methodology as yesterday)
- Live: `https://dogeswap.ag` (yesterday's build; `/chain-status` → `live:true`, block 6082878)

## Performance

Local harness (`npm run e2e:perf`, mocked API — directly comparable to yesterday):

| Metric | 2026-07-03 | 2026-07-02 | Delta |
| --- | ---: | ---: | ---: |
| Lighthouse performance | 78 (runs: 78 / 78 / 85) | 91 | -13 (see note) |
| Lighthouse accessibility | 92 | 92 | 0 |
| Quote to render | 296 ms (runs: 296 / 302 / 913) | 310 ms | -14 ms |
| Time to interactive | 100 ms | 100 ms | 0 |
| Load event | 106 ms | 107 ms | -1 ms |
| Bundle size | 14,578,457 bytes | 14,578,457 bytes | 0 |

**Note on the Lighthouse -13:** inconclusive, treated as measurement variance,
not filed as a regression. Evidence: bundle bytes are byte-identical to
yesterday, TTI/load/quote-to-render are unchanged, no web code merged today
besides the PR #5 tap-target tweaks (present in both measurements), and the
score swung 78→85 between consecutive runs on this host. If tomorrow's run
also lands < 85, file a `qa,regression` issue and bisect.

Live site (https://dogeswap.ag, deployed = yesterday's build; **new baselines**, no 2026-07-02 comparison exists):

| Metric | Mobile emulation (LH default) | Desktop preset |
| --- | ---: | ---: |
| Lighthouse performance | 57 / 64 (two runs) | 31 |
| Lighthouse accessibility | 87 | 88 |
| LCP | 2,582–3,339 ms | 4,425 ms |
| Total blocking time | ~3,400–3,523 ms | 883 ms |
| LH interactive | ~25,500–26,600 ms | 6,646 ms |
| CLS | 0.02–0.05 | — |

Root cause of the live scores: the 13.7 MB (3.87 MB gzip)
`sdk-wallet-provider` chunk (**issue #13**). Raw category JSON in
`e2e/artifacts/perf/live-lighthouse-run{1,2}.json`; local harness JSON in
`e2e/artifacts/perf/2026-07-03.json`.

Live API latency (20 sequential `/quote` WDOGE→USDC exactInput; raw data
`e2e/artifacts/live-api/quote-latency.json`):

| Endpoint | Timing |
| --- | --- |
| `/quote` | p50 1,125 ms · p95 1,410 ms · min 1,103 · max 2,389 · 20/20 HTTP 200 |
| `/tokens` | 55 ms first · 29–34 ms repeat (server already warm; cold-start not measurable without restart) |
| `/chain-status` | 677 ms |
| `/activity` (router) | 1,156 ms |
| `/sources` | 59 ms |
| `/venues` | **5,991 ms cold, 3,940 ms second, 39 ms warm** (issue #15) |

## Sweep results

### Run A — local, main @ 12c063a (`npm run e2e`)

**35/35 passed** across all five projects (desktop-chromium,
iphone-portrait, iphone-landscape, android-portrait, android-landscape).
Full swap flow (connect → quote → approve → swap → activity), quote-outage
retry state, slippage rejection, insufficient balance, disconnect,
overflow sweep, tap targets: all green.

### Run B — live https://dogeswap.ag (deployed UI bundle, API mocked via page.route)

**29/35 passed, 6 failed:**

| Failure | Projects | Triage |
| --- | --- | --- |
| `visible controls have usable touch targets` — chart chip 76×28, ⇄ direction button 30×30, 25/50/75/MAX chips 77×28 (below the 36×30 suite floor) | all 5 | **Fixed on main @ `ed685f2` (PR #5 enlarged these controls in `apps/web/src/ui/Slider.jsx` + `SwapView.jsx`), pending deploy.** Not filed. |
| `connects wallet, quotes, approves, swaps, and records activity` — click on Activity nav blocked: toast `anim-rise` subtree intercepts pointer events for the full 10 s action timeout | android-landscape only | **Flake with evidence**: 3/3 pass on `--repeat-each=3` rerun. Root cause is real, though — the toast stack (fixed bottom-center, z-index 200, pills `pointerEvents:auto`, 4.2 s dismiss each) overlaps the bottom nav in landscape and eats taps. **Issue #14** (S3). Screenshot: `e2e/artifacts/test-results/swap-flow-swap-flow-connec-89891--swaps-and-records-activity-android-landscape/test-failed-1.png` |

Note for tomorrow: the suite's tap-target floor is 36×30; the QA bar is 44 px.
Post-#5 controls sit at ~34–36 px height — passes the suite, still short of
44 px on the percent chips. Tracked as polish; revisit if mobile mis-taps show
up.

## Live-API functional findings

**Pending-deploy (fixed on main, NOT filed):**
1. **Fee-unit bug visible in live `/quote` score** — `feeCostInOutputToken`
   (28,987,883,057,442) exactly equals `totalFeeWei`: DOGE-wei fee subtracted
   directly from output-token units. **Fixed on main by PR #10 (fee-unit
   scoring fix), pending deploy.** On this testnet USDC is 18-decimals so
   `netOutput` stays positive/sane (1.2484e18 vs gross 1.2485e18) — the
   expected negative-netOutput symptom for 6-decimal outputs cannot reproduce
   with the current token set, but the wrong-unit subtraction is confirmed
   live.
2. **Undersized tap targets on live UI** — see sweep table; **fixed on main by
   PR #5 (`ed685f2`), pending deploy.**

**Healthy on live:** `/chain-status` live:true chain 6281971; `/tokens` 10
tokens with provenance; `/quote` returns a sane split route (MuchFi V3 +
Barkswap Algebra legs, positive netOutput, minAmountOut < amountOut, 5 s TTL);
`/quote` correctly 400s (`wrong-chain`) without `chainId`; `/activity` for the
router returns decoded Blockscout transactions; `/sources` lists active pools.
Raw responses in `e2e/artifacts/live-api/`.

**New issues (filed today):**
1. **#13 (S2, qa)** — 13.7 MB `sdk-wallet-provider` chunk makes live mobile
   LH-interactive ~26 s and desktop Lighthouse 31. The single biggest gap to
   the "extremely smooth and fast on mobile" bar. Not a regression (bundle
   byte-identical to yesterday) — a standing problem now quantified on live.
2. **#14 (S3, qa)** — toast stack intercepts bottom-nav taps in landscape
   (root cause of the android-landscape flake above).
3. **#15 (S3, qa)** — `/venues` cold request takes 4–6 s (blocks on
   verification snapshot build); warm requests 39 ms.

## Known tooling notes (not filed)

- `chrome-launcher` is imported by `e2e/perf/run.mjs` but undeclared in
  `package.json` (works only as a hoisted transitive dep of `lighthouse`).
  Already flagged in PR #5 review. Did not fail today; no `--no-save` install
  was needed.
- Yesterday's local LH baseline (91) came from a single run in the PR #5
  environment; today's three runs establish ±7 points of run-to-run variance
  on this host. Consider having the harness take the median of 3 Lighthouse
  runs.

## Verdict

- Functional: **PASS** on main (35/35). Live failures fully explained
  (5 pending-deploy + 1 evidenced flake).
- UI/UX: PASS on main; live bundle carries known, already-fixed tap-target
  issues awaiting deploy.
- Performance: local metrics flat vs yesterday (LH delta judged variance);
  live-site numbers are poor on mobile and desktop due to bundle weight —
  **#13 filed, high priority**.
- **Action required: deploy main.** The live site is carrying at least two
  user-visible defects (tap targets, fee-unit quote scoring) that are already
  fixed on main.
