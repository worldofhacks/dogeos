// App.jsx — root of the DogeSwap React UI. The Shell owns the device frame,
// header, nav, theming, and overlay slots.
import React from "react";

import Shell from "./Shell.jsx";

export default function App() {
  return <Shell />;
}
